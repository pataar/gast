// Package main is the entry point for gast (GitLab Activity Stream TUI),
// a terminal UI that displays a live-updating stream of GitLab dashboard activity.
package main

import "github.com/pataar/gast/cmd"

func main() {
	cmd.Execute()
}
